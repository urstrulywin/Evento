const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  // Tailwind CSS configuration for custom theme
  theme: {
    extend: {
      colors: {
        primary: "#1E40AF",
        secondary: "#F59E0B",
      },
    },
  },
};

export default config;
